import React from 'react';
import clsx from 'clsx';

import styles from './cart.scss';

const cx = clsx.bind(styles);

export default function Cart() {
  return <div className="wrapper-cart">Đây là trang giỏ hàng</div>;
}
